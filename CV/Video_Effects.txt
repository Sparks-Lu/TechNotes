alpha_op(operation='shave', thresh=0.5, amount=0.333344, invert=False, display='image', input_as_alpha=0)
Display and manipulate the alpha channel 显示和操作透明通道 

blink(interval)
Blinks the clip at an interval 视频一闪一闪

brightness(brightness=0.5)
Adjusts the brightness of the clip 明亮度 

cartoon(triplevel=0.99, diffspace=0.003)
Cartoonify video, with a form of edge detectection 制造卡通效果

charcoal()
Applies a charcoal effect 木炭效果

chroma(amount=0.15, color='#00ff00', blend=None)
Removes a color from the clip, making it transparent 删除背景色

contrast(amount=0.5)
Adjusts the contrast of the clip 调整对比度 

crop(top=0, left=0, bottom=0, right=0)
Crops the clip 裁剪视频 

cut(start=None, end=None, duration=None)
Trims the clip

dither(amount=0.104167)
Dithers the clip and reduces the number of available colors

property duration
Duration of the clip

dust(maxdiameter=2, maxcount=10)
Add dust and specks to the clip 增加灰尘和斑点

dynamic_threshold()
Applies a dynamic thresholding effect 动态阈值效果

edgeglow(threshold=0.5, upscale=0.5, downscale=0)
Adds glowing edges 边缘发光

extract_color(color='r')
Extracts either red, green, or blue from clip 提取背景色 

fadein(duration)
Fades the clip in 添加渐入效果

fadeout(duration)
Fades the clip out 添加渐出效果

flip(axis)
Flip the clip over an axis 水平或者上下翻转 

freeze(frame=0, toward='after')
freeze one frame for entire clip 冻结某一帧 

fx(name, params=None)
Adds any filter to a clip 添加滤镜，上百种滤镜效果

glow(blur=0.5)
Apply a glow effect 创造迷人的光芒

gradient(start_color='#ffffff', end_color='#000000', pattern='linear', start_opacity=0.5, end_opacity=0.5, start_x=0.5, start_y=0, end_x=0.5, end_y=1, offset=0, blend='normal')
Draws a gradient on top of the clip. 绘制渐变 

grain(noise=40, contrast=160, brightness=70)
Adds grain over the clip 在剪辑上添加纹理

grayscale()
Convert the clip to grayscale 转成灰度

hflip()
Flip the clip horizontally 水平翻转

hue(shift=0)
Adjusts the hue of the clip 调整色相

invert()
Inverts the colors of the clip 反色一个视频

loop()
Loops the clip indefinitely 循环

luma(start=None, end=None)
luminance()

Creates a luminance map of the clip 创建亮度图
mark_mask()

Sets the clip as a matte mask 设置matte 视频遮罩 
mirror(axis='horizontal', reverse=False)

Provides various mirror and image reversing effects.
提供各种镜像和图像反转效果。

move(sequence, repeat=False, cycle=0, mirror=False)
Moves the clip around. 移动或者变换大小一个视频
Takes a list of (keyframe, x, y, w, h) params At each target frame, the clip will be moved and optionally resized to the supplied x,y,w,h params.
You can use pixels or percentages (in quotes)

opacity(amount)
Sets the clip’s opacity 设置透明度

pixelize(width=0.1, height=0.1)
Pixelize the clip 像素化滤镜

position(x='0%', y='0%', w='100%', h='100%', opacity='100%', distort=1)
Positions and resizes the clip. Coordinates can be either in pixels or percent. 设定视频位置 To maintain aspect ration, set distort=False

posterize(levels=0.1)
Posterizes the clip by reducing the number of colors used. 减少颜色，海报化

qtext(text, color='#ffffff', bgcolor='0x00000000', halign='center', text_decoration='None', font_family='Sans', size=1080, style='normal', weight=400, bbox=(0, 0, '100%', '100%'))
Overlays text on a clip. 添加字体 

repeat(total)
Repeats the clip 重复片段一定次数 

resize(w='100%', h='100%', distort=True)
Resizes the clip. Coordinates can be either in pixels or percent. 设定视频长宽大小 To maintain aspect ration, set distort=False

rotate(amount=1, axis='x')
Rotate the clip along an axis 旋转视频

saturate(saturation=0.125)
Adjusts the saturation of the clip

set_duration(duration)
Sets the duration of the clip

set_mask_from_file(mask_path)
Sets the mask for the clip based on the grayscale of a video, image, or vidpy Clip 视频遮罩

set_offset(offset)
Sets the offset of the clip - determines when the clip will start playing.

set_trans(name, params={})
Adds any transition to a track 添加各种转场和混合效果

shake1s(scale=True)
shake the clip around. 晃动进入

sharpness(amount=0.3, size=0)
Sharpens the clips 锐化 

sobel()
Applies a sobel filter sobel 滤镜

softglow(blur=0.5, brightness=0.75, sharpness=0.85, blurblend=0)
Does softglow effect on highlights 柔和高光部分

spin(speed=1, axis='x')
Spin the clip continuously. But why? 绕着某一个轴，持续旋转一个视频

squareblur(size=0.5)
Blurs the clip 马赛克 

tag_name = 'producer'
text(text, color='#ffffff', bgcolor='0x00000000', olcolor='0x00000000', outline=0, halign='center', valign='middle', pad=0, font='Sans', size=1080, style='normal', weight=400, bbox=(0, 0, '100%', '100%'))
Overlays text on a clip. 添加文字 

threshold(threshold=0.5)
Thresholds the clip 阙值调整 

vflip()
Flip the clip vertically 上下翻转

vignette(aspect=0.5, clear=0, softness=0.6)
Applies a vignette effect 应用晕影效果

volume(db_amt)
Sets the volume for the clip withe (-)db adjustment 调整声音 db 
web_effects(html, duration=None, scale_width=1, scale_height=1)
利用网页特效，已经废弃了

zoominto()
scale into the clip . 15帧的范围逐渐放大

zoompan(origin, dest, start=0, end=- 1)
Zooms and pans the clip over time 放大缩小 Takes an origin and destination coordinates either in pixels or percent.

class composition.Profile
定义生成视频的帧率，长宽比，像素等
class composition.Track(clips=[], filters=[], type='video', transition=None, **kwargs)¶
class composition.Transition(props={})
两段重叠视频之间的切换效果或者混合模式
tag_name = 'transition'
